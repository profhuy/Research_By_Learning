# === OpenAI API configuration ===
# 1. Copy this file:   cp .env.example .env
# 2. Put your REAL key in .env (never in this example file).
# 3. .env is gitignored, so the key never reaches GitHub.
OPENAI_API_KEY=sk-REPLACE_ME
